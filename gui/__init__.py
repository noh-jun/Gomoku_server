"""Server management GUI (Tkinter) and the uvicorn lifecycle controller.

Nothing here contains game logic; the modules only start/stop the server and
render its state.  ``gui.controller`` is importable without Tkinter.
"""
